# Generated by webpack from src/. On conflict keep the local copy and rebuild
# with `npm run build` rather than hand-merging minified output.
# Requires once per clone: git config merge.ours.driver true
assets/main.js merge=ours
assets/tailwind.css merge=ours
